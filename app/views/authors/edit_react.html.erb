<div id="author-form-edit"></div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    console.log('🚀 AuthorForm React (Edit) loading...');

    // Debug: Check what's available in window
    console.log('Available React components:', {
      React: !!window.React,
      ReactDOM: !!window.ReactDOM,
      createRoot: !!window.createRoot,
      AuthorForm: !!window.AuthorForm,
      renderReactComponent: !!window.renderReactComponent
    });

    // Check if React and components are available
    if (!window.React) {
      console.error('❌ React not loaded');
      return;
    }

    if (!window.AuthorForm) {
      console.error('❌ AuthorForm component not loaded');
      return;
    }

    const container = document.getElementById('author-form-edit');
    if (!container) {
      console.error('❌ Container not found');
      return;
    }

    try {
      // Author data from Rails
      const author = <%= raw @author.to_json %>;
      console.log('📝 Author data:', author);

      // Create React element
      const element = React.createElement(AuthorForm, {
        author: author,
        isEdit: true
      });

      // Try different rendering methods based on what's available
      if (window.createRoot) {
        console.log('📦 Using React 19 createRoot (global)');
        const root = window.createRoot(container);
        root.render(element);
      } else if (window.ReactDOM?.createRoot) {
        console.log('📦 Using React 19 createRoot (ReactDOM)');
        const root = window.ReactDOM.createRoot(container);
        root.render(element);
      } else if (window.ReactDOM?.render) {
        console.log('📦 Using React 18 render');
        window.ReactDOM.render(element, container);
      } else {
        console.error('❌ No React rendering method available');
        container.innerHTML = '<div class="p-4 bg-yellow-100 border border-yellow-400 rounded">React rendering not available</div>';
      }

    } catch (error) {
      console.error('❌ AuthorForm rendering failed:', error);
      container.innerHTML = `<div class="p-4 bg-red-100 border border-red-400 rounded">Error: ${error.message}</div>`;
    }
  });
</script>

<style>
  .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem;
  }

  .form-group {
    margin-bottom: 1rem;
  }

  .form-label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
    color: #374151;
  }

  .form-input {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid #d1d5db;
    border-radius: 0.375rem;
    font-size: 1rem;
    transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
  }

  .form-input:focus {
    outline: none;
    border-color: #3b82f6;
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
  }

  .form-error {
    color: #ef4444;
    font-size: 0.875rem;
    margin-top: 0.25rem;
  }

  .btn {
    padding: 0.75rem 1.5rem;
    border-radius: 0.375rem;
    font-weight: 600;
    text-decoration: none;
    display: inline-block;
    text-align: center;
    transition: all 0.15s ease-in-out;
    cursor: pointer;
    border: none;
  }

  .btn-primary {
    background-color: #3b82f6;
    color: white;
  }

  .btn-primary:hover {
    background-color: #2563eb;
  }

  .btn-secondary {
    background-color: #6b7280;
    color: white;
    margin-left: 0.5rem;
  }

  .btn-secondary:hover {
    background-color: #4b5563;
  }

  .btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }
</style>
